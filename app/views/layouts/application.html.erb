<!DOCTYPE html>
<html>
  <head>
    <title>Kirocco</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="bg-light">
    <!-- 共通ヘッダー -->
    <nav class="navbar navbar-light bg-white border-bottom shadow-sm px-4">
      <div class="container-fluid d-flex justify-content-between align-items-center">
        <a class="navbar-brand fw-semibold" href="/">Kirocco</a>

        <div class="dropdown">
          <button
            class="btn btn-light border dropdown-toggle p-2"
            type="button"
            id="menuDropdown"
            data-bs-toggle="dropdown"
            aria-expanded="false">
            <span class="navbar-toggler-icon"></span>
          </button>

          <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="menuDropdown">
            <li><a class="dropdown-item" href="/shelves">My Page</a></li>
            <li><a class="dropdown-item" href="/settings">Settings</a></li>
            <li><hr class="dropdown-divider"></li>
            <li>
              <%= link_to "Logout",
                destroy_user_session_path,
                method: :delete,
                data: { turbo: false, confirm: "ログアウトしますか？" },
                class: "dropdown-item text-danger" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- ここに各ページの中身を差し込む -->
    <%= yield %>

    <%= javascript_importmap_tags %>
  </body>
</html>
